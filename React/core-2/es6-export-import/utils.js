console.log('utils.js running');

const square = (x) => x * x;

const add = (a, b) => a + b;

export { square, add };

// or export variables inline of declaration of variabnle
// export const add = (a, b) => a + b;
